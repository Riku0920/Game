using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class gamemanagemaent : MonoBehaviour
{
   /* //コンポーネントを取得
    sePlay = GameObject.Find("SePlayer").GetComponent<SePlay>();
    public void Sust()
    {
        if(gameObject == title)
        {
            //効果音名を指定して再生
            sePlay.Play("BGM0");
        }
    }
    */

}
